# Cascading context — v4.1

Later phases consume earlier structured artifacts; never contradict silently — surface merge.

---

## Priority influence chain

foundation → positioning (may merge!) → **[the_constraints]** early → plumbing → journeys → data → infra remainder → tiering → blueprint.

---

## Constraints-run-early rule (v4.1)

After foundation (and optional merged positioning) activate **`infrastructure/the_constraints`** ahead of integrations selection.

Downstream biases:

free-first → prefer OSS / generous free tiers; avoid proposing paid SaaS staples unless unavoidable with explicit flag.

moderate/high budget → can recommend managed services boldly.

timeline urgent → trims optional modules per anti-overwhelm first.

explicit stack affinity (e.g. “only React”) → filters suggestions and tutorial micro-lines.

Constraints feed into `the_plumbing` vendor/menu multi-select weighting — do not re-negotiate dollar caps later except user-initiated drift correction.
