# Example — Food delivery (near clone)

**Profile:** User names national delivery app repeatedly; Accelerator `full_clone` leaning.

## Trace highlights

1. Flow + screen map confirmations replace broad discovery — user tweaks cuisine niche + delivery SLA only.
2. `show_me_the_money` activates from reference monetization inference even before Medium tier classification borderline resolved upward.
3. Question volume drops ~55% versus greenfield Medium app; blueprint still contains `_meta.reference_app`.
