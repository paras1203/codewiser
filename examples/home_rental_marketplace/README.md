# Example — Home rental marketplace (partial clone)

**Profile:** Complex, Reference = Airbnb-like but for suburban guest suites.  
**Accelerator behavior:** Preloads two-sided booking, payouts, calendars; focuses questions on locality rules, dispute flavor, zoning nuance.

## Abbreviated engine trace

1. Reference classified `partial_clone` → diff checklist for trust, listing verification, payouts region.
2. `the_constraints` fixes moderate budget → plumbing biases to mainstream payment rails versus crypto experiments.
3. `feature_tiering` pushes social graph to Later; MVP holds search + booking + messaging.
4. Blueprint `_meta.reference_app="Airbnb(pattern)"`; `_meta.defaults_applied` empty except delegated notification defaults user confirmed.
