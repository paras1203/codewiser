# Example — Habit tracker (simple, merged phases)

**Profile:** Single user, Minimal/Simple complexity, no monetization baseline.

## Trace highlights

1. Early merge: `the_positioning` combined with `the_big_idea` in one confirmation bubble.
2. `success_metrics_and_horizon` **skipped** entirely (Simple) — no north-star prompts.
3. Feature tiering still runs but quick: habit logging + reminders = MVP; analytics + export = v1.1.
4. Session depth ~9 effective phase modules after merges.
