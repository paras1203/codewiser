# Look & Feel — AI synthesis

Infer palette/font/density cautiously from domain keywords; always label inference and seek confirm.

For **`reference_ui`:** if the user skips or leaves empty, set `No reference — AI should suggest`; otherwise `The UI should feel like [App Name]` with the named app.

Map outputs to `blueprint_blocks.design_ux` and **`DESIGN_BRIEF.md`** only — no tokens, component patterns, or accessibility rules in MVP.
