# Patterns — show_me_the_money

## PATTERN: monetization_rails

**priority_weight:** `{ "impact": 5, "dependency": 4, "uncertainty": 4 }`

**FORMAT:** Single choice → conditional multi  
Micro-education before named rails vocabulary. Single choice model then conditional multi rails.  
Gate payment rails on **`locale_context`** per **`_engine/locale_payment_rails.md`**.
