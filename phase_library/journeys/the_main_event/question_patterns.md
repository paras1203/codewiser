# The Main Event — question patterns

## PATTERN: core_journey_triple_confirm

**priority_weight:** `{ "impact": 5, "dependency": 5, "uncertainty": 3 }`

**FORMAT:** Confirm cluster  
TRIPLE CONFIRM clustering using reference baseline if present:

1 action core → success tableau → graceful failure/recovery.
