# Patterns — core_screen_deep_dive

## PATTERN: core_screen_checklist

**priority_weight:** `{ "impact": 4, "dependency": 4, "uncertainty": 3 }`

**FORMAT:** Confirm checklist per screen  
For each core screen (max 3 per cycle): confirm structure using checklist (header, primary CTA, secondary actions, empty state, loading pattern). Split across exchanges if needed.
