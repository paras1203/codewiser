# Patterns — the_blueprint

## PATTERN: blueprint_summary_confirm

**priority_weight:** `{ "impact": 5, "dependency": 5, "uncertainty": 2 }`

**FORMAT:** Confirm + delta  
CONFIRM large structured summary + targeted delta questions only if missing fields.

Medium complexity: embed **three** `success_metrics_and_horizon` questions here (`north_star_metric`, `month_1_target`, roadmap third) — not a separate phase.
