Do not run this synthesis for **minimal** or **simple** sessions (no standalone phase, no blueprint wedge).

When run (medium wedge or complex phase), write **`success_metrics.north_star_metric`**, **`month_1_target`**, and **`month_6_target`** (optional for medium; complex may expand). Normalize legacy keys `north_star` / `m1_target` into the canonical names above on write.

Mark speculative confidence low; funnel extras into **`roadmap`**.
