# Feature Tiering — extraction goals

Runs **always**, immediately before `the_blueprint`. Converts accumulated feature chatter into prioritized tiers.

Uses **Tiering Bucket** format: MVP | v1.1 | Later | Drop.

Output seeds `features` block with roadmap alignment.
