# The Constraints — AI synthesis

Apply **cascading bias**: free-first → demote paid vendor suggestions in `the_plumbing` unless security forces them (then surface trade-off).

Log default proposals with `_meta`.
