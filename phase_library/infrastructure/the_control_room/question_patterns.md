# Patterns — the_control_room

## PATTERN: admin_surface_confirm

**priority_weight:** `{ "impact": 3, "dependency": 3, "uncertainty": 4 }`

**FORMAT:** Role-gated confirm  
Role-gated confirm list; skip if no admin role.
