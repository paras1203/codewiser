# Patterns — the_plumbing

## PATTERN: integration_category_multiselect

**priority_weight:** `{ "impact": 4, "dependency": 4, "uncertainty": 4 }`

**FORMAT:** Multi-select  
Multi-select categories with micro-ed lines per category when needed. Pre-check obvious items from reference. Respects `constraints.free_first_bias`.
